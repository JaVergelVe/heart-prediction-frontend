/** Claves en `history.state` / `Location.getState()` al navegar al resultado. */
export const PREDICTION_ROUTER_STATE_KEYS = {
  flowKind: 'predictionFlowKind',
  result: 'predictionResult'
} as const;
